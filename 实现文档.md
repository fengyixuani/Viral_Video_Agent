# 通用爆款复刻程序复刻说明文档

> 本文是给研发和 AI Coding 使用的程序级复刻说明。目标是：只看本文档，就能从零搭出当前项目同等结构、同等页面、同等接口、同等 Agent 链路的原型服务，不需要二次澄清。
>
> 当前项目形态：一个可运行的 Python Web 服务，前端是网页；不是生成静态 HTML 文件。后端提供上传、Skill 列表、趋势挖掘、两段 SSE 流式 Agent 接口。前端展示三栏业务页面和底部全宽 Agent reasoning 面板。

---

## 1. 产品定义

### 1.1 一句话

用户提供一个参考爆款视频和自己的现有素材，系统先理解爆款为什么有效，拆出行业、分镜、爆点结构、可复刻维度和复刻方案；用户选择方案、勾选维度、按需补素材；Agent 再规划、逐镜决策、调用生成/剪辑/包装工具，输出复刻成片方案和占位成片。

### 1.2 用户主流程

1. 用户进入网页。
2. 用户选择一个高频场景 Skill，例如电商素材营销复刻、直播切片复刻、创意短剧复刻；也可以使用通用模式。
3. 用户上传或填写参考爆款视频 URL。
4. 用户一开始就上传自己的现有素材，包括图片、文本、视频；也可以在文本框补充卖点、脚本、商品信息。
5. 用户填写复刻意图，例如“复刻这个爆款，主推我的商品”。
6. 用户点击“理解爆款”。
7. 后端通过 AgentScope 调 Kimi-K2.6，流式输出 reasoning；前端底部面板实时展示思考过程。
8. 理解完成后，前端展示：
   - 中栏：逐镜拆解，每个镜头有可变 breakdown 维度。
   - 右栏：行业推断、热门趋势候选、2 到 3 个 LLM 生成的复刻方案、每个方案下的可复刻维度。
9. 用户勾选热门趋势关键词。
10. 用户选择一个复刻方案。
11. 用户勾选方案下的可复刻维度。
12. 对于 `replace.enabled=true` 的维度，用户可以补充素材，例如商品图、人物视频、口播文案。
13. 用户点击“生成复刻方案”。
14. 后端进入第二阶段 Agent：规划、素材盘点、逐镜 match/generate 决策、生成、剪辑、包装、评估。
15. 前端展示最终复刻方案、逐镜动作标签、计划、置信度、成片 URI 和当前 MOCK badcase。

### 1.3 当前原型边界

已接真实 LLM 的部分：

- 阶段一理解：行业、分镜、动态 breakdown、复刻方案、维度。
- 阶段二规划：plan-execute 计划。
- 阶段二逐镜决策：每个分镜用用户素材 match，还是 generate。
- 热门趋势候选：由 LLM 生成结构化趋势候选。

当前 MOCK 的部分：

- 视频多模态底层算子真实执行：镜头切分、VLM、ASR、OCR、情绪识别等。
- 用户素材画像。
- 视频生成。
- 剪辑编排。
- 字幕/TTS/音效/特效/导出包装。
- 复刻质量评估。

接真实能力时，优先替换对应 Tool 或 Operator 的 `run()` 内部逻辑，不改变 API、Agent、前端数据契约。

---

## 2. 技术选型和运行方式

### 2.1 技术栈

- Python：建议 3.12。
- Web 后端：Python 标准库 `http.server.ThreadingHTTPServer`。
- 前端：原生 HTML、CSS、JavaScript，文件在 `server/static/index.html`。
- Agent 模型框架：AgentScope。
- LLM：OpenAI 兼容接口，默认 `https://oneapi-comate.baidu-int.com/v1`。
- 默认模型：`Kimi-K2.6`。
- Skill 系统：AgentScope `LocalSkillLoader` 加载 `skills_defs/<id>/SKILL.md`。
- SSE：后端 `text/event-stream`，前端 `fetch().body.getReader()` 读取。

### 2.2 依赖

当前代码至少需要：

```bash
agentscope
python-frontmatter
```

建议安装命令：

```bash
python3.12 -m venv .venv
.venv/bin/pip install agentscope python-frontmatter
```

注意：

- 必须使用较新的 AgentScope 版本，当前代码按 AgentScope 2.x 风格实现。
- 不建议用系统 Python 3.9 跑；旧版本 Python 可能只能安装较旧 AgentScope，API 不匹配。

### 2.3 环境变量

`src/as_core.py` 读取以下环境变量：

```bash
LLM_BASE_URL=https://oneapi-comate.baidu-int.com/v1
LLM_API_KEY=<你的 key>
LLM_MODEL=Kimi-K2.6
PORT=8000
```

代码里已有默认值，但生产化不能硬编码 key。正式环境应通过环境变量或密钥服务注入。

### 2.4 启动

在项目根目录运行：

```bash
.venv/bin/python main.py
```

默认访问：

```text
http://127.0.0.1:8000
```

`main.py` 的职责非常薄：把 `server` 目录加入 `sys.path`，然后导入 `server/app.py` 的 `main()` 并启动服务。

---

## 3. 目录结构

AI Coding 从零复刻时，必须按这个结构组织。不要把前端做成单独静态导出，也不要把 Agent、Tool、Skill 全塞进一个文件。

```text
.
├── main.py
├── README.md
├── docs/
│   ├── 实现文档.md
│   ├── 研发落地文档.md
│   ├── 交互设计稿.html
│   ├── 界面原型.html
│   ├── 链路图.html
│   └── Agent编排图.html
├── server/
│   ├── app.py
│   └── static/
│       └── index.html
├── skills_defs/
│   ├── ecom_product/
│   │   └── SKILL.md
│   ├── live_clip/
│   │   └── SKILL.md
│   ├── drama/
│   │   └── SKILL.md
│   ├── knowledge/
│   │   └── SKILL.md
│   └── creative_ad/
│       └── SKILL.md
├── src/
│   ├── as_core.py
│   ├── operators.py
│   ├── profile.py
│   ├── schema.py
│   ├── serialize.py
│   ├── skills.py
│   ├── trends.py
│   ├── inputs/
│   │   ├── __init__.py
│   │   └── loader.py
│   ├── agent/
│   │   ├── __init__.py
│   │   ├── orchestrator.py
│   │   └── prompts.py
│   └── tools/
│       ├── __init__.py
│       ├── base.py
│       ├── understanding.py
│       ├── generation.py
│       ├── editing.py
│       └── packaging.py
└── uploads/
```

职责分层：

- `main.py`：入口，只负责启动服务。
- `server/app.py`：HTTP 路由、上传、SSE、静态页面服务。
- `server/static/index.html`：单页前端，包含页面结构、样式、交互、SSE 消费。
- `src/as_core.py`：AgentScope 模型内核，对外提供 `stream()`、`complete()`、`complete_json()`、`parse_json()`。
- `src/agent/orchestrator.py`：业务 Agent 编排，两阶段异步流式执行。
- `src/agent/prompts.py`：规划和逐镜决策提示词。
- `src/tools/understanding.py`：理解 Tool，负责构造理解提示词、定义 LLM 输出 schema、把 LLM JSON 转成 dataclass。
- `src/tools/generation.py`：生成 Tool，当前 MOCK。
- `src/tools/editing.py`：剪辑 Tool，当前 MOCK。
- `src/tools/packaging.py`：包装 Tool，当前 MOCK。
- `src/tools/base.py`：Tool 协议和 MockTool 基类。
- `src/operators.py`：底层多模态原子算子，当前 MOCK。
- `src/skills.py`：AgentScope Skill 加载、解析、列表输出。
- `skills_defs/*/SKILL.md`：可插拔 Skill 定义。
- `src/trends.py`：热门趋势候选模块。
- `src/profile.py`：行业 Profile。
- `src/schema.py`：核心数据契约。
- `src/serialize.py`：dataclass 转 JSON。
- `src/inputs/loader.py`：请求 payload 归一化为 `InputBundle`。
- `uploads/`：上传文件落盘目录，由服务启动或上传时自动创建。

---

## 4. 后端服务规格

### 4.1 服务入口

文件：`server/app.py`

关键全局对象：

```python
ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, os.path.join(ROOT, "src"))
STATIC_DIR = os.path.join(os.path.dirname(os.path.abspath(__file__)), "static")
UPLOAD_DIR = os.path.join(ROOT, "uploads")
os.makedirs(UPLOAD_DIR, exist_ok=True)
AGENT = ReplicationAgent()
```

服务绑定：

```python
port = int(os.getenv("PORT", "8000"))
srv = ThreadingHTTPServer(("127.0.0.1", port), Handler)
srv.serve_forever()
```

### 4.2 路由总览

```text
GET  /                 -> 返回 server/static/index.html
GET  /static/*         -> 返回静态资源
GET  /api/skills       -> 返回 Skill 列表
POST /api/upload       -> 上传本地文件，返回 video_uri
POST /api/trends       -> 获取热门趋势候选
POST /api/analyze      -> SSE，阶段一理解
POST /api/replicate    -> SSE，阶段二复刻
```

### 4.3 `GET /api/skills`

响应：

```json
{
  "skills": [
    {
      "id": "ecom_product",
      "name": "电商素材营销复刻",
      "icon": "...",
      "desc": "电商商品展示带货...",
      "industry": "ecom",
      "scheme_hint": "faithful",
      "preset_dims": ["coarse-structure"],
      "prompt_hint": "这是电商商品展示带货场景..."
    }
  ]
}
```

该接口直接调用 `skills.skill_list()`。前端启动后自动请求该接口渲染 Skill 推荐栏。

### 4.4 `POST /api/upload`

请求：`multipart/form-data`，字段名为 `file`。

实现方式：当前用标准库手动按 boundary 解析 multipart，找到包含 `filename=` 的 part，提取文件名和内容。

落盘：

```text
uploads/{uuid前8位}_{原始文件名basename}
```

响应：

```json
{
  "video_uri": "uploads/abcd1234_demo.mp4",
  "filename": "demo.mp4",
  "size": 123456
}
```

当前没有文件大小、类型、安全扫描、鉴权，原型可用，生产必须补齐。

### 4.5 `POST /api/trends`

请求：

```json
{
  "industry_id": "ecom",
  "intent": "复刻这个爆款，主推我的商品",
  "skill_id": "ecom_product"
}
```

后端逻辑：

1. 解析 JSON。
2. 如果传入 `skill_id`，调用 `skill_mod.get()` 找到 Skill。
3. 把 Skill 的 `prompt_hint` 拼成场景 hint。
4. 调用 `asyncio.run(trends.fetch_trends(industry_id, intent, hint))`。
5. 返回趋势列表。

响应：

```json
{
  "trends": [
    {
      "keyword": "黄金3秒",
      "phrase": "前3秒直接抛痛点",
      "source": "平台趋势",
      "reason": "短视频推荐场景下能提高完播启动率"
    }
  ]
}
```

### 4.6 `POST /api/analyze`

请求：

```json
{
  "video_uri": "uploads/xxx.mp4 或 https://...",
  "video_desc": "视频内容描述，可为空",
  "intent": "用户复刻意图，可为空",
  "skill_id": "ecom_product，可为空",
  "materials": ["uploads/a.png", "商品卖点文本"]
}
```

响应类型：SSE。

关键响应头：

```http
Content-Type: text/event-stream; charset=utf-8
Cache-Control: no-cache
X-Accel-Buffering: no
```

SSE data 每条是 JSON：

```text
data: {"type":"step",...}\n\n
data: {"type":"reasoning",...}\n\n
data: {"type":"analysis",...}\n\n
data: [DONE]\n\n
```

### 4.7 `POST /api/replicate`

请求：

```json
{
  "industry_id": "ecom",
  "template": {"template_id":"tpl-llm", "shot_slots": []},
  "selected_dimensions": ["fine-product", "fine-hook"],
  "selected_trends": ["前3秒直接抛痛点"],
  "material_strategy": "faithful",
  "scheme_name": "忠于素材方案",
  "intent": "主推我的商品",
  "materials": ["uploads/a.png", "商品卖点文本", "uploads/b.mp4"]
}
```

响应类型：SSE。

最终 `final` 事件结构：

```json
{
  "type": "final",
  "result": {
    "profile_industry": "ecom",
    "plan_spec": {
      "goal": "一句话复刻目标",
      "granularity": "action_scene",
      "reasoning": "规划理由",
      "steps": [{"tool":"解析", "purpose":"..."}]
    },
    "template": {"shot_slots": []},
    "selected_dimensions": ["fine-product"],
    "plan": {
      "plan_id": "plan-xxxxxxxx",
      "granularity": "action_scene",
      "confidence": 0.0,
      "source": "from-default",
      "slot_assignments": [
        {"slot_id": 1, "action": "generate", "material_clip_id": null, "gen_prompt": "..."}
      ],
      "packaging": {},
      "voice_strategy": {},
      "material_strategy": "faithful"
    },
    "video": {
      "uri": "mock://output/final.mp4",
      "duration_sec": 0.0,
      "shots": []
    },
    "report": {
      "plan_id": "plan-xxxxxxxx",
      "structure_score": 0.0,
      "badcases": ["MOCK: ..."]
    },
    "trace": []
  }
}
```

### 4.8 SSE 桥接实现

`server/app.py` 是同步 HTTP server，但 Agent 是 async generator。桥接方式：

```python
def _sse(self, make_gen):
    payload = json.loads(self._read_body() or b"{}")
    self.send_response(200)
    self.send_header("Content-Type", "text/event-stream; charset=utf-8")
    self.send_header("Cache-Control", "no-cache")
    self.send_header("X-Accel-Buffering", "no")
    self.end_headers()

    def emit(ev):
        self.wfile.write(f"data: {json.dumps(ev, ensure_ascii=False)}\n\n".encode("utf-8"))
        self.wfile.flush()

    bundle = load(payload)

    async def _drain():
        async for ev in make_gen(bundle):
            emit(ev)

    asyncio.run(_drain())
    self.wfile.write(b"data: [DONE]\n\n")
```

错误处理：如果发生异常，打印 traceback，并 emit：

```json
{"type":"error", "message":"错误信息"}
```

---

## 5. SSE 事件契约

前端只按 `type` 分发，不依赖 HTTP chunk 细节。

### 5.1 `reasoning`

用于展示 LLM 思考过程增量。

```json
{
  "type": "reasoning",
  "phase": "理解",
  "text": "增量 reasoning 文本"
}
```

常见 `phase`：

```text
理解
plan
解析
决策
生成
剪辑
包装
评估
```

前端映射：

```javascript
const LABEL = {理解:"理解", plan:"规划", 解析:"解析", 决策:"决策", 生成:"生成", 剪辑:"剪辑", 包装:"包装", 评估:"评估"};
```

### 5.2 `step`

用于展示 Agent 进度和工具调用结果。

```json
{
  "type": "step",
  "phase": "理解",
  "thought": "接收爆款视频，启动多模态理解...",
  "action": "tool: 解析(视频)",
  "observation": "可选观察结果"
}
```

前端行为：

- 如果当前 phase 有打开的 reasoning `<details>`，收到 step 后折叠该 thinking box。
- 如果 `observation` 或 `action` 存在，追加 trace 卡片。

### 5.3 `analysis`

阶段一结束事件。

```json
{
  "type": "analysis",
  "result": {
    "template": {},
    "industry_guess": "ecom",
    "industry_reason": "推断理由",
    "industry_options": [{"id":"ecom", "label":"电商营销"}],
    "schemes": [],
    "skill_id": "ecom_product"
  }
}
```

前端收到后调用 `renderAnalysis(result)`。

### 5.4 `final`

阶段二结束事件。

```json
{
  "type": "final",
  "result": {}
}
```

前端收到后调用 `renderFinal(result)`。

### 5.5 `error`

```json
{
  "type": "error",
  "message": "错误信息"
}
```

当前前端没有专门弹窗，只在 stream 异常时写 trace。AI Coding 可补一个 `error` 分支，但不要改变主契约。

### 5.6 流结束

```text
data: [DONE]
```

前端忽略 `[DONE]`。

---

## 6. 数据契约

文件：`src/schema.py`

### 6.1 `ShotSlot`

```python
@dataclass
class ShotSlot:
    id: int
    want: str
    duration: float
    role: str = ""
    breakdown: list = field(default_factory=list)
    narrative: dict = field(default_factory=dict)
    camera: dict = field(default_factory=dict)
    visual: dict = field(default_factory=dict)
    audio: dict = field(default_factory=dict)
```

字段说明：

- `id`：分镜编号。
- `want`：该镜头在复刻时想实现的内容，例如“前3秒痛点钩子”。
- `duration`：镜头时长，秒。
- `role`：镜头角色，例如 hook、product_intro、cta、reversal。
- `breakdown`：最重要字段。可变拆解维度，由 LLM 按行业、内容、镜头作用决定。
- `narrative/camera/visual/audio`：兼容旧结构，当前优先使用 `breakdown`。

`breakdown` 示例：

```json
[
  {"dim":"Hook形式", "value":"开头直接抛出用户痛点"},
  {"dim":"景别", "value":"商品手持特写"},
  {"dim":"字幕贴片", "value":"大字突出价格利益点"},
  {"dim":"BGM节奏", "value":"快节奏鼓点，配合快速切镜"}
]
```

重要约束：`breakdown[].dim` 名称要尽量与 `schemes[].dimensions[].name` 呼应。前端会用同名匹配高亮 `↔可复刻`。

### 6.2 `BaseTemplate`

```python
@dataclass
class BaseTemplate:
    template_id: str
    industry: str
    total_duration_sec: float
    hook: dict
    narrative_structure: list[str]
    rhythm: dict
    shot_slots: list[ShotSlot]
    selling_points_order: list[str] = field(default_factory=list)
    cta: dict = field(default_factory=dict)
    packaging: dict = field(default_factory=dict)
```

用于承载阶段一分析出的爆款模板。

### 6.3 `MaterialClip` 和 `MaterialProfile`

```python
@dataclass
class MaterialClip:
    clip_id: str
    uri: str
    tags: list[str]
    fillable_slots: list[str]
    quality: Literal["A", "B", "C"]
    duration: float

@dataclass
class MaterialProfile:
    material_id: str
    clips: list[MaterialClip]
    capability: dict = field(default_factory=dict)
```

当前 `UnderstandingTool.profile_materials()` 返回空 profile：

```python
MaterialProfile(material_id="mat-mock", clips=[], capability={})
```

真实接入后，素材画像应由 VLM/ASR/OCR/镜头切分算子生成。

### 6.4 `SlotAssignment`

```python
@dataclass
class SlotAssignment:
    slot_id: int
    action: Literal["match", "generate"]
    material_clip_id: Optional[str] = None
    gen_prompt: Optional[str] = None
```

- `match`：用用户素材匹配剪辑。
- `generate`：生成补足。

当前 MOCK 中，由于素材画像为空，默认更容易产生 `generate`。

### 6.5 `ReplicationPlan`

```python
@dataclass
class ReplicationPlan:
    plan_id: str
    granularity: Literal["full", "action_scene", "style"]
    confidence: float
    source: Literal["from-default", "clarified", "user-edited"]
    slot_assignments: list[SlotAssignment]
    packaging: dict = field(default_factory=dict)
    voice_strategy: dict = field(default_factory=dict)
    material_strategy: Literal["faithful", "regenerate"] = "faithful"
```

注意：`material_strategy` 类型标注里只有 `faithful|regenerate`，但 UI 和 LLM scheme 也可能使用 `balanced`。AI Coding 复刻时建议把类型扩展为：

```python
Literal["faithful", "balanced", "regenerate"]
```

如果保持现状，Python 运行不会因类型标注报错，但文义上应支持 balanced。

### 6.6 `VideoFile`

```python
@dataclass
class VideoFile:
    uri: str
    duration_sec: float
    shots: list[dict] = field(default_factory=list)
```

当前包装 Tool 返回：

```json
{"uri":"mock://output/final.mp4", "duration_sec":0.0, "shots":[]}
```

---

## 7. 输入归一化

文件：`src/inputs/loader.py`

`InputBundle` 是后端所有 API payload 进入 Agent 前的统一结构。

```python
@dataclass
class InputBundle:
    video_uri: str = ""
    video_desc: str = ""
    intent: str = ""
    skill_id: str = ""
    industry_id: str = "ecom"
    template: dict = field(default_factory=dict)
    selected_dimensions: list[str] = field(default_factory=list)
    selected_trends: list[str] = field(default_factory=list)
    materials: list[str] = field(default_factory=list)
    material_strategy: str = "faithful"
```

`load(payload)` 只做轻量归一化：

- 字符串字段默认空字符串。
- 列表字段默认空列表。
- `video_desc` 和 `intent` 做 `.strip()`。
- `industry_id` 默认 `ecom`。
- `material_strategy` 默认 `faithful`。

---

## 8. AgentScope 模型内核

文件：`src/as_core.py`

### 8.1 常量

```python
BASE_URL = os.getenv("LLM_BASE_URL", "https://oneapi-comate.baidu-int.com/v1")
API_KEY = os.getenv("LLM_API_KEY", "...")
MODEL = os.getenv("LLM_MODEL", "Kimi-K2.6")
```

### 8.2 初始化模型

```python
_model = None

def get_model() -> OpenAIChatModel:
    global _model
    if _model is None:
        _model = OpenAIChatModel(
            credential=OpenAICredential(api_key=API_KEY),
            model=MODEL,
            stream=True,
            client_kwargs={"base_url": BASE_URL},
        )
    return _model
```

设计点：

- 全局缓存 `_model`，避免每次请求重复构造模型客户端。
- `stream=True`，使 AgentScope 返回可异步迭代的流式结果。
- 通过 `client_kwargs={"base_url": BASE_URL}` 接入 oneapi-comate。

### 8.3 消息格式

AgentScope 新版本要求 content blocks：

```python
msgs = [
    Msg(name="system", content=[TextBlock(type="text", text=system)], role="system"),
    Msg(name="user", content=[TextBlock(type="text", text=user)], role="user"),
]
```

不要传 plain string content，否则流式 block 解析可能不符合预期。

### 8.4 解析 thinking 和 text

```python
def _split(chunk) -> tuple[str, str]:
    th, tx = "", ""
    for b in chunk.content or []:
        t = getattr(b, "type", None)
        if t == "thinking":
            th += getattr(b, "thinking", "") or ""
        elif t == "text":
            tx += getattr(b, "text", "") or ""
    return th, tx
```

AgentScope 流式 chunk 是累计值，不一定是增量。因此 `stream()` 要保存上一轮 thinking 全文，只输出增量。

### 8.5 对外 `stream()`

```python
async def stream(system: str, user: str):
    model = get_model()
    res = await model(msgs)
    prev_th, prev_tx = "", ""
    async for chunk in res:
        th, tx = _split(chunk)
        if len(th) > len(prev_th):
            yield {"reasoning": th[len(prev_th):]}
        prev_th, prev_tx = th, tx
    yield {"content": prev_tx}
```

输出约定：

- 中间多次 yield：`{"reasoning":"增量"}`。
- 最后一次 yield：`{"content":"完整正文"}`。

### 8.6 JSON 解析

```python
def parse_json(txt: str) -> dict:
    txt = (txt or "").strip()
    if txt.startswith("```"):
        txt = txt.split("```", 2)[1]
        if txt.startswith("json"):
            txt = txt[4:]
        txt = txt.strip()
    i = txt.find("{")
    if i == -1:
        raise ValueError("no JSON object found")
    obj, _ = json.JSONDecoder().raw_decode(txt[i:])
    return obj
```

必须用 `raw_decode`，不要简单 `json.loads(txt)`。原因：LLM 可能输出 JSON 后附带额外文本，`json.loads` 会报 `Extra data`。

### 8.7 便捷方法

```python
async def complete(system, user) -> str
async def complete_json(system, user) -> dict
```

`trends.py` 使用 `complete_json()` 获取趋势候选。

---

## 9. AgentScope Skill 复用方式

### 9.1 设计目标

Skill 系统直接复用 AgentScope，不自研一套 Skill DSL、注册中心或执行框架。项目里的 `src/skills.py` 只做很薄的适配：用 AgentScope `LocalSkillLoader` 扫描 `skills_defs/` 下的 `SKILL.md`，再把 frontmatter/正文整理成前端可展示、Agent 可注入 prompt 的结构。

用户想增加高频场景时，只需要新增一个 AgentScope Skill 文件。当前约定路径是 `skills_defs/<skill_id>/SKILL.md`；每个目录一个 `SKILL.md`，便于 `LocalSkillLoader(scan_subdir=True)` 枚举。删除对应目录就是卸载；重启服务或调用 `skills.reload()` 后生效。

用户点击 Skill 后，系统不改变主链路，只把该 `SKILL.md` 的正文作为场景提示注入理解阶段，让 LLM 更聚焦。

### 9.2 实现文件

文件：`src/skills.py`

这个文件不是新的 Skill 框架，只是 AgentScope Skill 的项目级读取适配层。核心依赖：

```python
import frontmatter
from agentscope.skill import LocalSkillLoader
```

### 9.3 Skill 展示数据类

```python
@dataclass
class Skill:
    id: str
    name: str
    icon: str
    desc: str
    industry: str
    scheme_hint: str = "faithful"
    operator_pipeline: list = field(default_factory=list)
    preset_dims: list = field(default_factory=list)
    prompt_hint: str = ""
```

`to_dict()` 返回给前端：

```python
{
  "id": id,
  "name": name,
  "icon": icon,
  "desc": desc,
  "industry": industry,
  "scheme_hint": scheme_hint,
  "preset_dims": preset_dims,
  "prompt_hint": prompt_hint
}
```

当前 `operator_pipeline` 未返回给前端，但内部可使用。AI Coding 可以保留当前行为，也可以返回它。

### 9.4 加载逻辑

```python
SKILL_DIR = <project_root>/skills_defs
loader = LocalSkillLoader(directory=SKILL_DIR, scan_subdir=True)
as_skills = asyncio.run(loader.list_skills())
dirs = [os.path.basename(s.dir.rstrip("/")) for s in as_skills]
```

关键点：必须设置 `scan_subdir=True`。否则 `skills_defs/<id>/SKILL.md` 不会被枚举。

如果 AgentScope loader 异常，则 fallback 到文件系统扫描：

```python
dirs = [d for d in os.listdir(SKILL_DIR)
        if os.path.isfile(os.path.join(SKILL_DIR, d, "SKILL.md"))]
```

然后用 `frontmatter.load(md)` 解析 frontmatter 和正文：

- frontmatter：结构化字段。
- markdown body：`prompt_hint`。

### 9.5 `SKILL.md` 格式

每个 Skill 就是一个 AgentScope `SKILL.md` 文件。当前项目约定每个 Skill 放在独立目录：

```text
skills_defs/<skill_id>/SKILL.md
```

用户新增 Skill 时，不需要改 Python 代码；按下面格式新增一个 `SKILL.md`，重启后就会被 AgentScope `LocalSkillLoader` 扫到，并出现在前端 Skill 推荐栏。

格式：

```markdown
---
name: 电商素材营销复刻
skill_id: ecom_product
icon: "..."
description: 电商商品展示带货，复刻痛点→商品→使用→卖点→CTA 结构，接用户商品素材
industry: ecom
scheme_hint: faithful
operator_pipeline: [shot_segment, vlm_tag, asr, ocr]
preset_dims: [coarse-structure, fine-product, fine-hook, fine-order, fine-cta]
---

这是电商商品展示带货场景（storyboard workflow）。固化流程：痛点场景→商品出现→多视角展示→使用演示→卖点→CTA。
重点关注：商品位置/多视角镜头/卖点顺序/贴片文字/价格标/CTA 形式。
用户通常有商品图/使用视频作为素材，方案应标注商品镜头和人物镜头为可替换维度。
```

字段说明：

- `name`：前端展示名。
- `skill_id`：接口和状态里使用的唯一 ID。
- `icon`：前端展示符号。也可以用纯文本代替。
- `description`：前端 Skill 描述。
- `industry`：该 Skill 默认行业。Agent 理解阶段会用它覆盖 LLM 推断行业。
- `scheme_hint`：该 Skill 倾向的策略，例如 `faithful`、`balanced`、`regenerate`。
- `operator_pipeline`：该场景建议使用的底层算子顺序。
- `preset_dims`：该场景建议关注的维度 ID。
- 正文：注入 LLM 的场景提示。

### 9.6 当前内置 Skill

当前有 5 个：

1. `ecom_product`：电商素材营销复刻。
   - 行业：`ecom`。
   - 策略：`faithful`。
   - 算子：`shot_segment, vlm_tag, asr, ocr`。
   - 重点：痛点、商品、多视角、使用演示、卖点、CTA。
2. `live_clip`：直播切片复刻。
   - 行业：`live`。
   - 策略：`faithful`。
   - 算子：`asr, emotion, shot_segment, ocr`。
   - 重点：高光检测、话术钩子、情绪峰值、前3秒 Hook、TTS Clone。
3. `drama`：创意短剧复刻。
   - 行业：`drama`。
   - 策略：`balanced`。
   - 算子：`shot_segment, scene_event, asr, vlm_tag`。
   - 重点：剧情节拍、冲突、反转、人设、换皮或风格迁移。
4. `knowledge`：知识科普复刻。
   - 行业：`knowledge`。
   - 策略：`balanced`。
   - 算子：`asr, ocr, vlm_tag`。
   - 重点：讲解结构、可视化配图节奏、信息密度。
5. `creative_ad`：品牌创意广告复刻。
   - 行业：`ecom`。
   - 策略：`regenerate`。
   - 算子：`shot_segment, vlm_tag, asr, emotion`。
   - 重点：创意钩子、情感共鸣、软性商品植入。

### 9.7 Skill 在 Agent 中的使用

`ReplicationAgent.analyze_stream()` 中：

```python
skill = get_skill(bundle.skill_id) if bundle.skill_id else None
skill_hint = f"\n\n[Skill 场景提示]\n{skill.prompt_hint}" if skill else ""
override_industry = skill.industry if skill else None
messages = self.understanding.analyze_messages(
    bundle.video_uri,
    bundle.video_desc + skill_hint,
    bundle.intent,
    bundle.materials,
)
industry = override_industry or data.get("industry_guess", "ecom")
```

因此：

- 选择 Skill 后，LLM 会收到场景提示。
- Skill 的 `industry` 会覆盖 LLM 的行业推断。
- 主流程仍然是通用两阶段，不为每个 Skill 写一套独立 API。

---

## 10. 算子层 Operators

文件：`src/operators.py`

### 10.1 设计定位

Operator 是多模态原子能力，比 Tool 更细。

- Tool：Agent 直接调用的业务能力，例如解析、生成、剪辑、包装。
- Operator：Tool 内部组合使用的信号处理能力，例如切镜、ASR、OCR、VLM 打标。

当前 Operator 全部 MOCK。接真实能力时，只替换每个 Operator 的 `run()`。

### 10.2 当前算子

注册表：

```python
REGISTRY = {
    "shot_segment": ShotSegmentOperator(),
    "vlm_tag": VLMTagOperator(),
    "asr": ASROperator(),
    "ocr": OCROperator(),
    "emotion": EmotionOperator(),
    "scene_event": SceneEventOperator(),
}
```

算子说明：

- `shot_segment`：镜头切分。输入视频 URI，输出 `[{start,end,transition_type}]`。
- `vlm_tag`：逐镜多模态打标。输入帧或片段，输出景别、主体、商品位置、动作等。
- `asr`：语音识别和说话节奏分析。输出全文、词级 segments、pace。
- `ocr`：字幕、贴片、价格标识别。输出每帧文字和 bbox。
- `emotion`：声纹和情绪识别。输出 speaker_id 和 emotion_curve。
- `scene_event`：场景/事件切分。输出 scene_id、start、end、event_type。

### 10.3 真实模型替换建议

- `shot_segment`：AutoShot、OmniShotCut、TransVLM。
- `vlm_tag`：Qwen-VL、InternVL、其他多模态模型。
- `asr`：FunASR、Whisper。
- `ocr`：PaddleOCR 或 OCR 云服务。
- `emotion`：MiniCPM-o、Qwen3-Omni。
- `scene_event`：Scene-VLM、Marlin 系列。

---

## 11. 行业 Profile

文件：`src/profile.py`

### 11.1 目标

行业差异应该主要是数据，不是分叉代码。Agent 阶段二按 `industry_id` 加载 Profile，Tool 可根据 Profile 决定权重、工具倾向、评估 rubric。

### 11.2 Profile 数据类

```python
@dataclass
class Profile:
    industry: str
    label: str = ""
    dimension_weights: dict = field(default_factory=dict)
    toolkit: dict = field(default_factory=dict)
    judge_rubric: dict = field(default_factory=dict)
    workflows: list = field(default_factory=list)
```

### 11.3 当前行业

- `ecom`：电商营销。
- `live`：直播带货切片。
- `drama`：短剧。
- `knowledge`：知识科普。

前端行业下拉来自：

```python
INDUSTRY_OPTIONS = [{"id": p.industry, "label": p.label} for p in REGISTRY.values()]
```

`load_profile(industry_id)` 找不到时默认返回 `ECOM`。

---

## 12. Tool 层

### 12.1 Tool 协议

文件：`src/tools/base.py`

```python
class Tool(Protocol):
    name: str
    def run(self, payload: Any, profile: Any) -> Any:
        ...
```

`MockTool` 提供 `_log()`：

```python
line = f"[MOCK tool:{self.name}] {msg}"
print(line)
```

### 12.2 UnderstandingTool

文件：`src/tools/understanding.py`

这是当前唯一真正参与 LLM 结构化输出的 Tool。注意：它不直接调 LLM，而是负责组 prompt；真正流式调用在 Orchestrator 里做。

核心方法：

```python
schema_hint(self) -> str
analyze_messages(video_uri, video_desc, intent, materials) -> list[dict]
build_template(data, industry) -> BaseTemplate
profile_materials(materials) -> MaterialProfile
```

#### 12.2.1 `schema_hint()`

必须要求 LLM 输出严格 JSON：

```json
{
  "industry_guess": "ecom|live|drama|knowledge",
  "industry_reason": "str",
  "total_duration_sec": 0,
  "hook": {"type":"str", "duration_sec":0},
  "narrative_structure": ["str"],
  "rhythm": {"avg_shot_sec":0, "cut_density":"str"},
  "selling_points_order": ["str"],
  "cta": {"type":"str", "text":"str"},
  "shot_slots": [
    {
      "id": 1,
      "want": "str",
      "duration": 3.0,
      "role": "str",
      "breakdown": [{"dim":"str", "value":"str"}]
    }
  ],
  "schemes": [
    {
      "id": "str",
      "name": "str",
      "strategy": "faithful|balanced|regenerate",
      "desc": "str",
      "dimensions": [
        {
          "id": "str",
          "name": "str",
          "level": "coarse|fine",
          "desc": "str",
          "recommended": true,
          "replace": {
            "enabled": true,
            "types": ["image|text|video"],
            "hint": "str"
          }
        }
      ]
    }
  ]
}
```

#### 12.2.2 理解提示词重点

系统提示必须包含这些要求：

1. 你是通用爆款视频拆解专家。
2. 推断行业：`ecom/live/drama/knowledge`。
3. 逐分镜拆解 `shot_slots`。
4. `breakdown` 是可变维度，不是固定四栏。
5. 维度由行业、内容、镜头作用决定。
6. 每个分镜给 5 到 9 个有信息量的维度。
7. `breakdown[].dim` 名称要尽量与 `schemes[].dimensions[].name` 呼应。
8. 生成 2 到 3 组复刻方案。
9. 每组方案自主命名。
10. `strategy` 取 `faithful/balanced/regenerate`。
11. 方案内维度要从整体到细节，分 coarse/fine。
12. 需要用户补素材的维度，`replace.enabled=true`。
13. 系统自动处理的维度，`replace.enabled=false`。
14. 结合用户现有素材判断哪些已有料、哪些需补。
15. 严格输出 JSON。
16. `shot_slots` 给 4 到 6 个镜头。

#### 12.2.3 `build_template()`

将 LLM dict 转为 `BaseTemplate`：

- 遍历 `data["shot_slots"]`。
- 构造 `ShotSlot`。
- `breakdown=s.get("breakdown", []) or self._compat_breakdown(s)`。
- 保留 `total_duration_sec`、`hook`、`narrative_structure`、`rhythm`、`selling_points_order`、`cta`、`packaging`。

#### 12.2.4 `_compat_breakdown()`

兼容旧结构：如果 LLM 返回旧的 `narrative/camera/visual/audio`，转换成 breakdown：

```python
[
  {"dim":"叙事", "value":"scene:..., role:..."},
  {"dim":"镜头语言", "value":"shot_size:..., composition:..."}
]
```

#### 12.2.5 `profile_materials()`

当前实现：

```python
return MaterialProfile(material_id="mat-mock", clips=[], capability={})
```

真实版应调用 Operators：

1. 对用户视频素材做 shot_segment。
2. 对视频帧做 vlm_tag。
3. 对音频做 asr 和 emotion。
4. 对关键帧做 ocr。
5. 汇总为可填槽位 `fillable_slots`。
6. 计算素材质量 A/B/C。

### 12.3 GenerationTool

文件：`src/tools/generation.py`

当前 MOCK：

```python
return {
    "uri": "mock://generated/placeholder.mp4",
    "duration": payload.get("duration", 0.0),
    "prompt": prompt,
    "engine": "seedance-mock",
}
```

真实版应接 Seedance 或 Wan，输入至少包含：

- `gen_prompt`。
- 时长。
- 画幅。
- 用户补充素材。
- 风格约束。
- 参考镜头 breakdown。

### 12.4 EditingTool

文件：`src/tools/editing.py`

当前 MOCK：

```python
return {"timeline": [], "note": "剪辑能力未接入，返回空时间线"}
```

真实版应实现：

- 用 `slot.want` 和 breakdown 检索用户素材。
- 对齐镜头时长。
- 做节奏卡点。
- 输出 timeline。
- 支持 match 素材和 generated 素材混排。

### 12.5 PackagingTool

文件：`src/tools/packaging.py`

当前 MOCK：

```python
return VideoFile(uri="mock://output/final.mp4", duration_sec=0.0, shots=payload.get("shots", []))
```

真实版应实现：

- 字幕。
- TTS 或 TTS Clone。
- 音效。
- BGM。
- 转场。
- 调色。
- 特效。
- 平台适配导出。

---

## 13. Agent 编排

文件：`src/agent/orchestrator.py`

核心类：`ReplicationAgent`

```python
class ReplicationAgent:
    def __init__(self):
        self.understanding = UnderstandingTool()
        self.generation = GenerationTool()
        self.editing = EditingTool()
        self.packaging = PackagingTool()
```

### 13.1 `_llm_json()`

通用结构化 LLM 调用。

```python
async def _llm_json(self, phase, system, user):
    content = ""
    async for d in as_core.stream(system, user):
        if "reasoning" in d and d["reasoning"]:
            yield {"type":"reasoning", "phase":phase, "text":d["reasoning"]}
        elif "content" in d:
            content = d["content"]
    try:
        data = as_core.parse_json(content) if content.strip() else {}
    except Exception:
        data = {}
    yield {"type":"__json__", "data":data}
```

设计点：

- reasoning 不留在后端，直接转发给前端。
- 正文累计后做 JSON 解析。
- 解析失败返回 `{}`，避免整条 SSE 中断。
- 内部 sentinel 类型 `__json__` 不发给前端，由调用方消费。

### 13.2 阶段一：`analyze_stream(bundle)`

执行顺序：

1. 根据 `bundle.skill_id` 获取 Skill。
2. 把 Skill 正文作为 `[Skill 场景提示]` 拼进 `video_desc`。
3. 发出 step：启动多模态理解。
4. 调 `UnderstandingTool.analyze_messages()` 生成 system/user prompt。
5. 调 `_llm_json("理解", system, user)`。
6. 将 reasoning 事件逐条 yield 给 SSE。
7. 取得 LLM JSON。
8. 行业：如果选了 Skill，则 `skill.industry` 覆盖 LLM 结果；否则使用 `data.industry_guess`。
9. 用 `build_template(data, industry)` 生成 `BaseTemplate`。
10. 从 data 里取 `schemes`。
11. 发出 step：推断行业、分镜数量、方案数量、维度数量。
12. 发出 `analysis` 事件。

`analysis.result` 必须包含：

```python
{
    "template": to_jsonable(template),
    "industry_guess": industry,
    "industry_reason": data.get("industry_reason", ""),
    "industry_options": INDUSTRY_OPTIONS,
    "schemes": schemes,
    "skill_id": bundle.skill_id,
}
```

### 13.3 阶段二：`replicate_stream(bundle)`

执行顺序：

1. `profile = load_profile(bundle.industry_id)`。
2. `template = understanding.build_template(bundle.template, bundle.industry_id)`。
3. `sel = bundle.selected_dimensions`。
4. 发出 step：根据勾选维度规划复刻步骤。
5. 构造 `plan_user`，包含：
   - 行业。
   - 复刻意图。
   - 用户勾选复刻维度。
   - 用户勾选热门趋势因素。
   - 素材数。
   - 复刻策略。
6. 调 `_llm_json("plan", PLAN_SYSTEM, plan_user)`。
7. 得到 `plan_spec`，若为空则 fallback。
8. 发出 step：LLM 规划完成。
9. 调 `understanding.profile_materials(bundle.materials)`，当前返回空素材画像。
10. 发出 step：盘点用户素材能力。
11. 构造 `slots_brief = [{slot_id, want}]`。
12. 构造 `decide_user`，包含策略、勾选维度、素材片段数、分镜摘要。
13. 调 `_llm_json("决策", DECIDE_SYSTEM, decide_user)`。
14. 汇总 decisions。
15. 调 `_build_plan()` 生成 `ReplicationPlan`。
16. 统计 match/generate 数量。
17. 发出 step：逐镜决策结果。
18. 对所有 generate 分镜调用 `GenerationTool.run()`。
19. 如果有生成片段，发出 step：生成。
20. 调 `EditingTool.run()`。
21. 发出 step：剪辑。
22. 调 `PackagingTool.run()`。
23. 发出 step：包装。
24. 构造 MOCK report。
25. 发出 step：评估。
26. 发出 `final` 事件。

### 13.4 `_build_plan()`

逻辑：

```python
for each shot_slot:
    action = decisions.get(slot_id).get("action")
    if action not in ("match", "generate"):
        if no material_profile.clips:
            action = "generate"
        elif bundle.material_strategy == "faithful":
            action = "match"
        else:
            action = "generate"
```

然后生成 `SlotAssignment`：

- `match`：`SlotAssignment(s.id, "match", material_clip_id=None)`。
- `generate`：`SlotAssignment(s.id, "generate", gen_prompt=s.want)`。

`confidence` 当前计算为 match 占比：

```python
confidence = round(match_count / len(assignments), 2)
```

---

## 14. Agent 提示词

文件：`src/agent/prompts.py`

### 14.1 `PLAN_SYSTEM`

规划 Agent 的职责：根据参考爆款、素材和复刻意图，产出 plan-execute 计划。

必须声明可用工具：

- 解析。
- 生成。
- 剪辑。
- 包装。

必须解释策略：

```text
faithful=尽量用用户素材(优先剪辑)
regenerate=尽量重新生成
```

建议 AI Coding 复刻时补充 `balanced=素材和生成平衡`，因为当前 scheme 支持 balanced。

输出 JSON：

```json
{
  "goal": "一句话复刻目标",
  "granularity": "full|action_scene|style",
  "reasoning": "为什么这样规划(2-3句)",
  "steps": [
    {"tool":"解析|生成|剪辑|包装", "purpose":"这步做什么"}
  ]
}
```

### 14.2 `DECIDE_SYSTEM`

逐镜决策 Agent 的职责：为每个分镜决定 `match` 或 `generate`。

输出 JSON：

```json
{
  "decisions": [
    {"slot_id": 1, "action":"match|generate", "reason":"原因"}
  ]
}
```

---

## 15. 热门趋势模块

文件：`src/trends.py`

### 15.1 目标

在理解完成后，额外给用户一组可勾选的热门爆款因素，来源标签包括：

- 爆款库沉淀。
- 网络热点。
- 平台趋势。

当前用 LLM 生成。未来可接真实联网搜索、平台趋势接口、内部爆款库检索。

### 15.2 Prompt 输出

系统要求严格输出：

```json
{
  "trends": [
    {
      "keyword": "str",
      "phrase": "str",
      "source": "爆款库沉淀|网络热点|平台趋势",
      "reason": "str"
    }
  ]
}
```

数量：8 到 12 条。

### 15.3 归一化

`fetch_trends()` 只保留 dict 项，并归一化字段：

```python
{
    "keyword": t.get("keyword", "").strip(),
    "phrase": t.get("phrase", "").strip(),
    "source": t.get("source", "平台趋势").strip(),
    "reason": t.get("reason", "").strip(),
}
```

最后过滤空 keyword。

### 15.4 进入复刻链路

前端用户勾选趋势后，将 `phrase || keyword` 加入 `selectedTrends`。

阶段二请求中传：

```json
{"selected_trends": ["前3秒直接抛痛点"]}
```

`replicate_stream()` 会拼入规划输入：

```python
trends_line = f"用户勾选要融入的热门趋势因素：{bundle.selected_trends}\n"
```

---

## 16. 前端页面规格

文件：`server/static/index.html`

### 16.1 页面原则

- 页面是实际可用的工作台，不是落地页。
- 首屏就是完整工具界面。
- 三栏独立滚动。
- Agent reasoning 面板在底部全宽，不挤在左栏。
- Oii logo 已移除，只显示“爆款复刻”。
- 用户现有素材和参考爆款并列，都是一开始输入。
- 理解之后再解锁右侧方案选择。
- 分镜拆解维度横向滚动，避免一行太宽撑破页面。

### 16.2 全局 CSS 变量

```css
:root {
  --bg:#000;
  --panel:#0c0c0e;
  --panel2:#141416;
  --line:#232327;
  --txt:#e8e8ea;
  --sub:#8a8a92;
  --dim:#5f5f66;
  --accent:#ff2d9b;
  --accent2:#c026d3;
  --vio:#8b5cf6;
  --ok:#10b981;
  --think-h:240px;
}
```

### 16.3 页面布局

顶栏：

```html
<div class="topbar">
  <span class="chip" ...>爆款复刻</span>
  <span class="mock-flag">Kimi-K2.6 reasoning · 理解/规划/决策接 LLM · 生成/剪辑/包装 MOCK</span>
</div>
```

主体三栏：

```css
.cols {
  display:flex;
  height:calc(100vh - 56px - var(--think-h));
  overflow:hidden;
}
.col {
  overflow-y:auto;
  overflow-x:hidden;
  height:100%;
  padding:16px;
}
.col-left { width:320px; flex:none; }
.col-mid { flex:1; min-width:360px; }
.col-right { width:420px; flex:none; }
```

底部思考面板：

```html
<div class="think-panel" id="thinkPanel">
  <div class="think-bar" id="thinkBar">
    <b>Agent 思考过程</b>
    <span>（reasoning 流式，点此展开/收起）</span>
    <span id="thinkToggle">▾</span>
  </div>
  <div class="trace" id="trace"></div>
</div>
```

收起逻辑：

```javascript
document.getElementById("thinkBar").onclick = () => {
  const c = document.body.classList.toggle("think-collapsed");
  document.documentElement.style.setProperty("--think-h", c ? "40px" : "240px");
  document.getElementById("thinkToggle").textContent = c ? "▴" : "▾";
};
```

### 16.4 左栏内容

左栏包含：

1. 高频场景 Skill 推荐栏。
2. 参考爆款视频上传或 URL。
3. 视频内容描述。
4. 用户现有素材上传。
5. 文本素材输入。
6. 复刻意图。
7. “理解爆款”按钮。

关键 DOM：

```html
<div id="skillShelf"></div>
<div id="skillDesc"></div>
<div class="drop" id="drop">上传本地视频</div>
<input type="file" id="file" accept="video/*">
<input id="url" placeholder="或 视频 URL：https://...">
<textarea id="desc"></textarea>
<div class="drop" id="matdrop">上传素材</div>
<input type="file" id="matfile" accept="image/*,video/*,.txt" multiple>
<div id="matlist"></div>
<textarea id="mattext"></textarea>
<textarea id="intent"></textarea>
<button id="analyze">理解爆款</button>
```

### 16.5 中栏内容

中栏展示“01. 拆片分析”。

`renderShots(tpl)` 渲染：

- 左侧镜头缩略图占位。
- 镜头编号。
- `want`。
- `duration` 和 `role`。
- 如果复刻完成，展示 `用素材` 或 `生成` 标签。
- 右侧横向滚动的 `breakdown` 维度卡片。

横向滚动样式：

```css
.dimstrip {
  display:flex;
  gap:10px;
  overflow-x:auto;
  flex:1;
  padding-bottom:6px;
}
.dimcell {
  flex:none;
  width:158px;
}
```

与可复刻维度呼应高亮：

```javascript
function replicableDimNames(){
  const names = new Set();
  ((analysis && analysis.schemes) || []).forEach(sc =>
    (sc.dimensions || []).forEach(d => names.add((d.name || "").trim()))
  );
  return names;
}
```

如果 `breakdown[].dim` 命中 scheme dimension name，则给 `.dimcell.echo` 并显示 `↔可复刻`。

### 16.6 右栏内容

右栏初始 locked：

```html
<div id="custom" class="locked">
```

理解完成后解除：

```javascript
document.getElementById("custom").classList.remove("locked");
```

右栏包含：

1. 行业下拉。
2. 行业推断理由。
3. 热门趋势 chips。
4. 复刻方案卡片。
5. 方案下的可复刻维度勾选列表。
6. 生成复刻方案按钮。
7. 最终结果区。

### 16.7 前端状态变量

```javascript
let videoUri = "";
let analysis = null;
let currentScheme = null;
let activeSkillId = "";
let userMaterials = [];
let dimMaterials = {};
let selectedTrends = [];
```

说明：

- `videoUri`：上传参考视频后后端返回的 URI。
- `analysis`：阶段一结果。
- `currentScheme`：用户选中的复刻方案。
- `activeSkillId`：用户选中的 Skill。
- `userMaterials`：一开始上传的素材 URI。
- `dimMaterials`：针对特定维度补充的素材，结构 `{dimId:[uri]}`。
- `selectedTrends`：用户勾选的趋势短语。

### 16.8 上传逻辑

通用上传：

```javascript
async function uploadFile(f){
  const fd = new FormData();
  fd.append("file", f);
  const r = await fetch("/api/upload", {method:"POST", body:fd});
  const d = await r.json();
  return d.video_uri;
}
```

参考视频：

- 点击 `#drop` 触发 `#file.click()`。
- 上传成功后设置 `videoUri`。
- `drop` 添加 `.has`。

用户素材：

- 点击 `#matdrop` 触发 `#matfile.click()`。
- 多选上传，每个文件调用 `uploadFile()`。
- 结果 push 到 `userMaterials`。

文本素材：

```javascript
function collectTextMaterials(){
  return document.getElementById("mattext").value
    .split("\n")
    .map(s => s.trim())
    .filter(Boolean);
}
```

所有素材：

```javascript
function allMaterials(){
  const dm = Object.values(dimMaterials).flat();
  return [...userMaterials, ...collectTextMaterials(), ...dm];
}
```

### 16.9 Skill 推荐栏

前端启动后立即请求：

```javascript
const r = await fetch("/api/skills");
const d = await r.json();
```

渲染 `.skill-card`，点击后：

```javascript
activeSkillId = c.dataset.id;
document.getElementById("skillDesc").textContent = c.dataset.desc;
```

还要额外添加“通用模式”卡片：

```html
<div class="skill-card" data-id="" data-desc="通用模式，不限场景">通用模式</div>
```

### 16.10 SSE 消费器

核心函数：

```javascript
async function stream(url, payload, onEvent){
  const r = await fetch(url, {
    method:"POST",
    headers:{"Content-Type":"application/json"},
    body:JSON.stringify(payload)
  });
  const reader = r.body.getReader();
  const dec = new TextDecoder();
  let buf = "";
  while(true){
    const {value, done} = await reader.read();
    if(done) break;
    buf += dec.decode(value, {stream:true});
    const parts = buf.split("\n\n");
    buf = parts.pop();
    for(const p of parts){
      const l = p.trim();
      if(!l.startsWith("data:")) continue;
      const data = l.slice(5).trim();
      if(data === "[DONE]") continue;
      handle(JSON.parse(data));
    }
  }
}
```

`handle(ev)` 逻辑：

- `reasoning`：按 phase 创建或追加 `<details class="think">`。
- `step`：折叠该 phase 的 thinking box，并追加 trace 卡片。
- 其他事件：折叠全部 thinking box，交给 `onEvent(ev)`。

解析异常要 `console.error("SSE parse/handle err", ...)`，避免页面静默卡住。

### 16.11 阶段一按钮逻辑

点击“理解爆款”：

```javascript
const payload = {
  video_uri: videoUri || document.getElementById("url").value,
  video_desc: document.getElementById("desc").value,
  intent: document.getElementById("intent").value,
  skill_id: activeSkillId,
  materials: allMaterials()
};
await stream("/api/analyze", payload, ev => {
  if(ev.type === "analysis") {
    analysis = ev.result;
    renderAnalysis(ev.result);
  }
});
```

按钮状态：

- 请求中：disabled，文案“理解中…”。
- 完成：文案“重新理解”。

### 16.12 `renderAnalysis(res)`

必须做这些事：

1. `renderShots(res.template)`。
2. 渲染行业下拉。
3. 显示 `industry_reason`。
4. 调 `renderSchemes(res.schemes || [])`。
5. 维度区显示“请先选择一个复刻方案”。
6. 解锁右栏。
7. 更新右栏状态文案。
8. 禁用“生成复刻方案”按钮，直到选方案。
9. 清空旧结果。
10. 下一帧异步调用 `loadTrends()`。

关键点：

```javascript
setTimeout(loadTrends, 0);
```

这样趋势挖掘失败或慢，不会阻塞方案卡片渲染。

### 16.13 `loadTrends()`

请求：

```javascript
fetch("/api/trends", {
  method:"POST",
  headers:{"Content-Type":"application/json"},
  body:JSON.stringify({
    industry_id: document.getElementById("industry").value,
    intent: document.getElementById("intent").value,
    skill_id: activeSkillId
  })
});
```

渲染：

- 每个趋势是 `.chip-t`。
- 点击 toggle `.on`。
- 选中时 push `t.phrase || t.keyword` 到 `selectedTrends`。
- 取消时从 `selectedTrends` 删除。
- `trendState` 显示已选数量。

### 16.14 `renderSchemes(schemes)`

必须在函数内部定义：

```javascript
const STRAT_LABEL = {faithful:"忠于素材", balanced:"平衡", regenerate:"重新生成"};
```

这是一个曾经导致右栏卡住的 bug 点。不要漏掉。

渲染 scheme card：

- 名称：`s.name || 方案N`。
- 策略标签：`STRAT_LABEL[s.strategy] || s.strategy`。
- 描述：`s.desc`。

点击 scheme 后：

```javascript
currentScheme = schemes[+el.dataset.i];
renderDims(currentScheme.dimensions || []);
document.getElementById("replicate").disabled = false;
```

### 16.15 `renderDims(dims)`

每个维度渲染为 checkbox：

- value：`d.id || d.name || index`。
- label：`d.name`。
- level badge：coarse 显示粗粒度，其他显示细粒度。
- `recommended=true` 默认 checked，并显示推荐。
- desc 显示 `d.desc`。

如果 `d.replace.enabled=true`：

- 显示“补素材”按钮。
- 隐藏 file input。
- 支持 multiple。
- 上传成功后写入 `dimMaterials[dim]`。
- 显示已补数量。

如果 `replace.enabled=false`：

- 显示“系统处理，无需/无法补素材”。

### 16.16 阶段二按钮逻辑

点击“生成复刻方案”：

```javascript
const dims = [...document.querySelectorAll(".dimchk:checked")].map(c => c.value);
const payload = {
  industry_id: document.getElementById("industry").value,
  template: analysis.template,
  selected_dimensions: dims,
  selected_trends: selectedTrends,
  material_strategy: (currentScheme && currentScheme.strategy) || "faithful",
  scheme_name: (currentScheme && currentScheme.name) || "",
  intent: document.getElementById("intent").value,
  materials: allMaterials()
};
await stream("/api/replicate", payload, ev => {
  if(ev.type === "final") renderFinal(ev.result);
});
```

### 16.17 `renderFinal(d)`

必须做：

1. 建立 `window._assign`：`slot_id -> action`。
2. 调 `renderShots(d.template)`，使中栏分镜出现 `用素材`/`生成` 标签。
3. 渲染 plan：
   - `plan_spec.goal`。
   - `granularity`。
   - 勾选维度。
   - 规划理由。
   - 步骤链路。
   - plan_id。
   - confidence。
   - material_strategy。
   - video.uri。
   - report.badcases。

---

## 17. 前端视觉和交互验收

AI Coding 复刻页面时，必须满足：

1. 顶栏高度 56px，标题是“爆款复刻”，不出现 Oii 标。
2. 页面主体是三栏，不是营销 landing page。
3. 左栏宽 320px，右栏宽 420px，中栏自适应。
4. 三栏各自滚动，页面整体不被中栏撑开。
5. 底部 reasoning 面板全宽，高度默认 240px，可折叠到 40px。
6. reasoning 是流式追加，不是等结束后一次性显示。
7. 每个 reasoning phase 用 `<details>`，新 phase 出现时折叠旧 phase。
8. step 卡片显示 phase、action、thought、observation。
9. 理解前右栏 locked，不可交互。
10. 理解完成后右栏解锁，先展示方案，不被趋势请求阻塞。
11. 分镜 breakdown 横向滚动，每个维度固定宽度 158px。
12. scheme dimension 与 breakdown 同名时高亮。
13. 选方案后才出现维度勾选。
14. 只有 `replace.enabled=true` 的维度显示补素材入口。
15. 生成后中栏每个分镜显示 match/generate 标签。

---

## 18. 从零复刻的实施顺序

AI Coding 应按以下顺序实现，避免前后契约错位。

### 18.1 建基础结构

1. 创建目录：`server/static`、`src/agent`、`src/tools`、`src/inputs`、`skills_defs`、`uploads`、`docs`。
2. 写 `main.py`，只负责启动 `server/app.py`。
3. 写 `src/schema.py`。
4. 写 `src/serialize.py`。
5. 写 `src/profile.py`。
6. 写 `src/inputs/loader.py`。

### 18.2 接 AgentScope 模型

1. 写 `src/as_core.py`。
2. 使用 `OpenAICredential`、`OpenAIChatModel`、`Msg`、`TextBlock`。
3. 实现 `_split()`，从 block 中取 `thinking` 和 `text`。
4. 实现 `stream()`，将累计 thinking 转增量。
5. 实现 `parse_json()`，用 `raw_decode`。

### 18.3 复用 AgentScope Skill

1. 写 `src/skills.py` 作为薄适配层，不自研 Skill 执行框架。
2. 用 `LocalSkillLoader(directory=SKILL_DIR, scan_subdir=True)` 扫描 AgentScope `SKILL.md`。
3. 用 `frontmatter.load()` 读取 `SKILL.md` 的元信息和正文，整理成前端展示数据。
4. 实现 `reload()`、`get()`、`skill_list()`。
5. 创建 5 个内置 `skills_defs/<id>/SKILL.md`。用户后续新增场景时，只新增对应 `SKILL.md` 即可。

### 18.4 写 Operators 和 Tools

1. 写 `src/operators.py`，注册 6 个 MOCK 算子。
2. 写 `src/tools/base.py`。
3. 写 `src/tools/understanding.py`。
4. 写 `src/tools/generation.py`。
5. 写 `src/tools/editing.py`。
6. 写 `src/tools/packaging.py`。
7. 写 `src/tools/__init__.py`，导出 Tool 类。

### 18.5 写 Agent

1. 写 `src/agent/prompts.py`。
2. 写 `src/agent/orchestrator.py`。
3. 实现 `_llm_json()`。
4. 实现 `analyze_stream()`。
5. 实现 `replicate_stream()`。
6. 实现 `_build_plan()`。
7. 写 `src/agent/__init__.py` 导出 `ReplicationAgent`。

### 18.6 写 Trends

1. 写 `src/trends.py`。
2. 通过 `as_core.complete_json()` 获取趋势候选。
3. 输出 `keyword/phrase/source/reason`。

### 18.7 写后端

1. 写 `server/app.py`。
2. 实现静态页面服务。
3. 实现 `/api/skills`。
4. 实现 `/api/upload`。
5. 实现 `/api/trends`。
6. 实现 `/api/analyze` SSE。
7. 实现 `/api/replicate` SSE。
8. 实现 async generator 到 sync HTTP 的 `asyncio.run(_drain())` 桥接。

### 18.8 写前端

1. 写 `server/static/index.html`。
2. 写三栏和底部 reasoning 面板结构。
3. 写 CSS。
4. 写上传逻辑。
5. 写 Skill 加载。
6. 写 SSE 消费器。
7. 写阶段一 analyze 逻辑。
8. 写 `renderShots()`。
9. 写 `renderAnalysis()`。
10. 写 `loadTrends()`。
11. 写 `renderSchemes()`。
12. 写 `renderDims()`。
13. 写阶段二 replicate 逻辑。
14. 写 `renderFinal()`。

### 18.9 验证

1. 启动服务。
2. 打开页面。
3. 确认 Skill 推荐栏出现 5 个 Skill 和通用模式。
4. 上传参考视频和素材，或输入 URL。
5. 点击理解爆款。
6. 确认底部 reasoning 流式输出。
7. 确认中栏出现分镜 breakdown。
8. 确认右栏出现行业、趋势、2 到 3 个方案。
9. 点击方案，确认维度勾选和补素材按钮出现。
10. 勾选趋势和维度，点击生成复刻方案。
11. 确认底部出现 plan/决策/生成/剪辑/包装/评估步骤。
12. 确认最终结果区出现 plan_id、confidence、mock 成片 URI。

---

## 19. 验收标准

### 19.1 服务验收

- `.venv/bin/python main.py` 能启动。
- 浏览器打开 `http://127.0.0.1:8000` 能看到页面。
- `GET /api/skills` 返回非空 skills。
- `POST /api/upload` 能返回 `uploads/...`。
- `POST /api/analyze` 返回 SSE，包含 reasoning 和 analysis。
- `POST /api/replicate` 返回 SSE，包含 reasoning/step/final。

### 19.2 Agent 验收

- reasoning 能流式输出。
- LLM 正文能被 `parse_json()` 解析。
- JSON 后有额外文字时不会 `Extra data`。
- 选择 Skill 后，分析结果的 industry 使用 Skill 行业。
- `schemes` 至少 2 组，最多建议 3 组。
- 每个 scheme 有 dimensions。
- 每个 shot 有 breakdown。
- breakdown 维度和 scheme dimension 能部分同名呼应。
- replicate 阶段能输出 `slot_assignments`。

### 19.3 前端验收

- 右栏不再卡在“解析完素材”状态。
- `STRAT_LABEL` 在 `renderSchemes()` 内定义，不报 ReferenceError。
- 趋势请求慢或失败时，方案仍先渲染。
- 维度补素材后显示已补数量。
- 生成后分镜上出现“用素材”或“生成”标签。
- 思考面板可展开/收起。

---

## 20. 常见故障和修复

### 20.1 AgentScope API 不匹配

现象：导入 `agentscope.credential`、`agentscope.model` 失败，或 `OpenAIChatModel` 参数不匹配。

原因：Python 版本太旧，安装了旧 AgentScope。

修复：使用 Python 3.12 重新建 venv，并安装新版本 AgentScope。

```bash
python3.12 -m venv .venv
.venv/bin/pip install agentscope python-frontmatter
.venv/bin/python main.py
```

### 20.2 Skill 列表为空

原因：`LocalSkillLoader` 没有扫描子目录。

修复：确保：

```python
LocalSkillLoader(directory=SKILL_DIR, scan_subdir=True)
```

并确认每个 Skill 路径是：

```text
skills_defs/<id>/SKILL.md
```

### 20.3 右栏方案不渲染

现象：理解完成后，右栏没有出现方案，控制台报 `STRAT_LABEL is not defined`。

修复：在 `renderSchemes()` 内部定义：

```javascript
const STRAT_LABEL = {faithful:"忠于素材", balanced:"平衡", regenerate:"重新生成"};
```

### 20.4 趋势挖掘影响方案展示

现象：理解完成后右栏被趋势请求拖住。

修复：`renderAnalysis()` 中先渲染 scheme 和解锁右栏，再：

```javascript
setTimeout(loadTrends, 0);
```

### 20.5 JSON 解析报 Extra data

原因：LLM 输出 JSON 后又输出了解释文字。

修复：使用 `json.JSONDecoder().raw_decode(txt[i:])` 取第一个完整 JSON 对象。

### 20.6 reasoning 不显示或一次性显示

检查：

1. `OpenAIChatModel(stream=True)`。
2. Msg content 使用 `[TextBlock(...)]`。
3. `_split()` 正确读取 `type == "thinking"` 的 block。
4. `as_core.stream()` 对累计 thinking 做增量 diff。
5. 后端每个 SSE 事件后 `flush()`。
6. 前端用 `reader.read()` 逐 chunk 解析。

### 20.7 页面分镜维度过宽

修复：维度容器必须横向滚动：

```css
.dimstrip { display:flex; overflow-x:auto; }
.dimcell { flex:none; width:158px; }
```

### 20.8 上传失败

当前 multipart 解析很轻量，复杂文件名或大文件可能有问题。原型够用，生产建议改为框架实现或严格 multipart parser。

---

## 21. 生产化改造清单

当前项目是原型。若上线，需要补：

1. API 鉴权。
2. 上传文件大小限制。
3. 文件类型校验。
4. 病毒/安全扫描。
5. 上传文件清理策略。
6. LLM key 从密钥系统读取。
7. 请求超时和取消。
8. SSE 心跳。
9. 后台任务队列。
10. 多用户隔离。
11. 日志链路 ID。
12. 真实素材画像。
13. 真实视频生成。
14. 真实剪辑 timeline。
15. 真实包装导出。
16. 真实 judge。
17. 错误 UI。
18. 自动测试。
19. 前端拆分和构建。
20. 模型输出 JSON schema 校验。

---

## 22. 最小可用代码行为摘要

如果 AI Coding 只记住这一段，也必须实现：

1. 服务启动后打开网页，不是输出静态 HTML 文件。
2. 页面左栏输入参考视频和用户素材，中栏展示分镜，右栏展示行业、趋势、方案、维度和结果，底部展示 reasoning。
3. `/api/analyze` 是 SSE，调用 AgentScope Kimi 流式 reasoning，最终返回 `analysis`。
4. `/api/replicate` 是 SSE，继续调用 AgentScope 做规划和逐镜决策，生成/剪辑/包装先 MOCK，最终返回 `final`。
5. Skill 基于 AgentScope `LocalSkillLoader` 从 `skills_defs/<id>/SKILL.md` 加载，前端可一键选择。
6. 分镜 breakdown 是可变维度，由 LLM 按行业和镜头作用决定；维度名要和方案里的可复刻维度呼应。
7. 方案是 LLM 生成的 2 到 3 组，不是固定按钮；每组下有 dimensions；`replace.enabled=true` 的维度允许用户补素材。
8. 趋势模块单独请求 `/api/trends`，用户勾选后注入 replicate 阶段。
9. Tool 和 Operator 都保留清晰边界，真实能力接入时替换 `run()`，不改主链路。
10. 所有 JSON 输出必须用鲁棒 parser 解析，防止 LLM 输出额外文本导致崩溃。
